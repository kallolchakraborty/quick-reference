{
  "id": "py-basics-api",
  "title": "API Handling",
  "category": "Python",
  "subcategory": "Basics",
  "description": "Making HTTP requests, handling responses, parsing JSON and XML data, setting headers, managing errors, and working with authentication.",
  "language": "python",
  "sections": [
    {
      "title": "HTTP GET Requests",
      "description": "Perform basic HTTP GET requests to fetch resources, handle parameters, check status codes, and parse JSON payloads.",
      "codeBlock": "import requests\n\n# Basic GET Request\nresponse = requests.get(\"https://api.github.com/users/python\")\nprint(response.status_code)       # 200\nprint(response.ok)                # True (status code < 400)\nprint(response.headers[\"Content-Type\"])\n\n# Parse response JSON\ndata = response.json()            # Decoded dictionary / list\nprint(data[\"login\"])\nprint(data[\"public_repos\"])\n\n# Request with Query Parameters (?q=python&page=1)\nparams = {\"q\": \"python\", \"page\": 1}\nresp = requests.get(\"https://api.github.com/search/repositories\", params=params)"
    },
    {
      "title": "HTTP POST Requests",
      "description": "Submit data payloads to APIs using HTTP POST requests by passing dictionaries to the json parameter.",
      "codeBlock": "import requests\n\npayload = {\"title\": \"Bug fix\", \"body\": \"Fixed the issue\"}\n\n# Send JSON payload\nresp = requests.post(\"https://api.example.com/issues\", json=payload)\nprint(resp.status_code)           # 201 Created"
    },
    {
      "title": "Request Headers & Timeouts",
      "description": "Configure request behaviors by adding HTTP headers, passing bearer tokens, and setting request timeout limits to prevent hanging.",
      "codeBlock": "import requests\n\n# Custom headers & authorization bearer token\nheaders = {\n    \"Authorization\": \"Bearer YOUR_TOKEN\",\n    \"Accept\": \"application/json\"\n}\nresp = requests.get(\"https://api.example.com/secure\", headers=headers)\n\n# Set Timeout limit (in seconds)\nresp = requests.get(\"https://api.example.com\", timeout=10)"
    },
    {
      "title": "HTTP Error Handling",
      "description": "Use raise_for_status() to automatically raise HTTPError exceptions on 4xx/5xx status codes, and catch errors using try/except blocks.",
      "codeBlock": "import requests\n\ntry:\n    resp = requests.get(\"https://api.example.com/data\", timeout=5)\n    resp.raise_for_status()       # Raises HTTPError if status is 4xx/5xx\n    data = resp.json()\nexcept requests.exceptions.Timeout:\n    print(\"Request timed out\")\nexcept requests.exceptions.HTTPError as e:\n    print(f\"HTTP error occurred: {e}\")\nexcept requests.exceptions.RequestException as e:\n    print(f\"Ambiguous request failed: {e}\")"
    },
    {
      "title": "XML Parsing (ElementTree)",
      "description": "Parse XML string schemas or local files using ElementTree. Access tags, attributes, and search for nodes with find/findall.",
      "codeBlock": "import xml.etree.ElementTree as ET\n\nxml_data = \"\"\"<book><title>Python 101</title><price>29.99</price></book>\"\"\"\n\n# Parse from string representation\nroot = ET.fromstring(xml_data)\nprint(root.tag)                   # \"book\"\ntitle = root.find(\"title\").text   # \"Python 101\"\nprice = root.find(\"price\").text   # \"29.99\"\n\n# Parse from local file\ntree = ET.parse(\"data.xml\")\nfile_root = tree.getroot()\nfor item in file_root.findall(\".//item\"):\n    print(item.text)"
    }
  ],
  "details": "<p class=\"mb-3\">The <code>requests</code> library is the de facto standard for HTTP in Python. It provides a clean, human-readable API for GET, POST, PUT, DELETE, PATCH, and HEAD requests. Responses expose <code>.status_code</code>, <code>.headers</code>, <code>.json()</code>, <code>.text</code>, and <code>.content</code> (raw bytes). The library automatically handles connection pooling, cookie persistence, redirect following, and gzip/deflate decompression. It is not part of the standard library — install with <code>pip install requests</code>.</p>\n\n<p class=\"mb-3\">For URL-only use (standard library), <code>urllib.request</code> provides lower-level HTTP access but requires more boilerplate for headers, encoding, and error handling. For async HTTP, see <code>httpx</code> (drop-in <code>requests</code>-compatible API with async/await support) or <code>aiohttp</code> for fully asynchronous workflows.</p>\n\n<p class=\"mb-3\"><strong>JSON parsing</strong> via <code>response.json()</code> deserializes the response body into native Python types (<code>dict</code>, <code>list</code>, <code>str</code>, <code>int</code>, <code>float</code>, <code>bool</code>, <code>None</code>) according to RFC 7159. The standard library <code>json</code> module provides identical functionality via <code>json.loads()</code> for string input and <code>json.load()</code> for file input. For high-throughput JSON processing, <code>orjson</code> and <code>simdjson</code> offer significant performance improvements.</p>\n\n<p class=\"mb-3\"><strong>XML parsing</strong> using <code>xml.etree.ElementTree</code> (standard library) provides a lightweight, Pythonic API. For advanced XML needs — schema validation (XSD), XPath/XSLT transformations, or DTD parsing — consider <code>lxml</code>, which is faster and more feature-complete. XML differs from JSON in supporting attributes (<code>&lt;item id=\"1\"&gt;</code>), namespaces (<code>xmlns</code>), mixed content, and document type declarations.</p>\n\n<p class=\"mb-3\">For data interchange format selection: JSON is preferred for REST APIs, configuration files, and most web services due to its simplicity and native JavaScript support. XML remains common in enterprise SOAP APIs, legacy systems, and document-centric workflows (SVG, RSS/Atom, Office Open XML). For structured tabular data, CSV or Parquet may be more appropriate.</p>\n\n<p class=\"mb-3\">See the <a href=\"https://requests.readthedocs.io/\" target=\"_blank\" rel=\"noopener noreferrer\" class=\"text-brand-500 hover:text-brand-600 transition-colors\">requests library documentation</a>, the <a href=\"https://docs.python.org/3/library/json.html\" target=\"_blank\" rel=\"noopener noreferrer\" class=\"text-brand-500 hover:text-brand-600 transition-colors\">json module</a>, <a href=\"https://docs.python.org/3/library/xml.etree.elementtree.html\" target=\"_blank\" rel=\"noopener noreferrer\" class=\"text-brand-500 hover:text-brand-600 transition-colors\">xml.etree.ElementTree</a>, and <a href=\"https://lxml.de/\" target=\"_blank\" rel=\"noopener noreferrer\" class=\"text-brand-500 hover:text-brand-600 transition-colors\">lxml</a> for complete documentation.</p>"
}