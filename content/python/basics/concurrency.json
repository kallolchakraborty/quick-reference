{
  "id": "concurrency",
  "title": "Multithreading & Concurrency",
  "category": "Python",
  "subcategory": "Basics",
  "description": "An interactive guide to Python concurrency models, comparing synchronous execution, multithreading, asynchronous programming (asyncio), and multiprocessing.",
  "codeBlock": "# 1. Multithreading (I/O Bound)\nimport threading\n\ndef fetch_data():\n    print(\"Fetching...\")\n\nt = threading.Thread(target=fetch_data)\nt.start()\n\n# 2. Multiprocessing (CPU Bound)\nimport multiprocessing\n\ndef compute_heavy():\n    print(\"Computing...\")\n\np = multiprocessing.Process(target=compute_heavy)\np.start()\n\n# 3. Asyncio (Cooperative Single-Threaded)\nimport asyncio\n\nasync def fetch_async():\n    await asyncio.sleep(1)\n    print(\"Done\")\n\nasyncio.run(fetch_async())",
  "details": "<p class=\"mb-4\">Python provides multiple concurrency models to write responsive and high-throughput applications. Choosing the right concurrency mechanism depends primarily on whether your workload is <strong>I/O-Bound</strong> (network, database, file system wait times) or <strong>CPU-Bound</strong> (heavy numerical operations, calculations, image resizing).</p><h4 class=\"text-xs font-mono font-bold uppercase tracking-wider text-orange-600 dark:text-orange-400 mb-2\">The Four Concurrency Paradigms</h4><ul class=\"list-disc list-inside mb-4 space-y-1.5 text-xs text-inkLight dark:text-gray-300\"><li><strong>Synchronous (Single-Thread):</strong> Executing one instruction after another. If an I/O blocking operation occurs, the entire program pauses and waits.</li><li><strong>Multithreading (`threading`):</strong> Running multiple threads within a single process. Due to CPython's Global Interpreter Lock (GIL), only one thread executes Python bytecode at a time, making it excellent for interleaving I/O-bound wait times but useless for parallel CPU scaling.</li><li><strong>Asynchronous (`asyncio`):</strong> Single-threaded cooperative multitasking utilizing an Event Loop. Tasks explicitly yield control back to the loop using the <code>await</code> keyword while waiting for I/O, avoiding OS thread context-switch overheads.</li><li><strong>Multiprocessing (`multiprocessing`):</strong> Spawning independent OS processes, each with its own CPython interpreter instance and GIL. This allows processes to run in parallel on separate physical CPU cores, making it the correct choice for CPU-bound computation.</li></ul><h4 class=\"text-xs font-mono font-bold uppercase tracking-wider text-orange-600 dark:text-orange-400 mb-2\">CPython Concurrency & Evolution Reference Specs</h4><ul class=\"list-disc list-inside space-y-1.5 text-xs text-inkLight dark:text-gray-300\"><li><a href=\"https://docs.python.org/3/library/concurrency.html\" target=\"_blank\" rel=\"noopener noreferrer\" class=\"text-brand-500 hover:text-brand-600 transition-colors font-semibold\">Python Concurrency Documentation</a> — Official CPython libraries for threading, multiprocessing, and concurrent futures.</li><li><a href=\"https://docs.python.org/3/library/asyncio.html\" target=\"_blank\" rel=\"noopener noreferrer\" class=\"text-brand-500 hover:text-brand-600 transition-colors font-semibold\">Asyncio Event Loop Specifications</a> — Dynamic runtime documentation detailing asynchronous cooperative routines.</li></ul><p class=\"mt-4\">For an interactive visual comparison between Synchronous, Multithreaded, Asynchronous, and Parallel execution models, see the <a href=\"concurrency.html\" class=\"text-brand-500 font-bold underline hover:text-brand-600 transition-colors\">Interactive Concurrency Guide</a>.</p>"
}
