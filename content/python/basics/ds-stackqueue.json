{
  "id": "py-ds-stackqueue",
  "title": "Stacks & Queues",
  "category": "Python",
  "subcategory": "Data Structures",
  "description": "Stacks and queues appear in nearly every FAANG interview \u2014 often as the hidden backbone of a 'hard' problem. The monotonic stack and queue-based BFS patterns are must-knows.",
  "language": "python",
  "sections": [
    {
      "title": "Stack & Queue Implementations",
      "description": "Python <code>list</code> as stack: O(1) amortized append/pop from right. <code>collections.deque</code> as queue: O(1) append/popleft. Never use <code>list</code> as a queue — <code>list.pop(0)</code> is O(N) due to shifting.<div style=\"margin:20px 0;display:flex;justify-content:center\"><svg width=\"400\" height=\"150\" viewBox=\"0 0 400 150\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%\"><defs><style>.sq-box{fill:var(--bg-secondary);stroke:var(--border-default);stroke-width:1.5}.sq-edge{stroke:var(--brand-primary);stroke-width:1.5;marker-end:url(#arrow-sq)}.sq-lbl{font:12px monospace;fill:#475569;text-anchor:middle;dominant-baseline:central}.sq-lbl-val{font:bold 12px monospace;fill:var(--text-primary);text-anchor:middle;dominant-baseline:central}.sq-lbl-title{font:bold 11px system-ui;fill:var(--brand-primary);text-anchor:middle}</style><marker id=\"arrow-sq\" viewBox=\"0 0 10 10\" refX=\"6\" refY=\"5\" markerWidth=\"5\" markerHeight=\"5\" orient=\"auto-start-reverse\"><path d=\"M0,2 L8,5 L0,8 z\" fill=\"var(--brand-primary)\"/></marker></defs><!-- STACK --><text x=\"100\" y=\"15\" class=\"sq-lbl-title\">STACK (LIFO)</text><line x1=\"60\" y1=\"30\" x2=\"60\" y2=\"130\" stroke=\"var(--border-default)\" stroke-width=\"2\"/><line x1=\"140\" y1=\"30\" x2=\"140\" y2=\"130\" stroke=\"var(--border-default)\" stroke-width=\"2\"/><line x1=\"60\" y1=\"130\" x2=\"140\" y2=\"130\" stroke=\"var(--border-default)\" stroke-width=\"2\"/><rect x=\"70\" y=\"100\" width=\"60\" height=\"20\" rx=\"3\" class=\"sq-box\"/><text x=\"100\" y=\"110\" class=\"sq-lbl-val\">Val A</text><rect x=\"70\" y=\"75\" width=\"60\" height=\"20\" rx=\"3\" class=\"sq-box\"/><text x=\"100\" y=\"85\" class=\"sq-lbl-val\">Val B</text><path d=\"M100,25 C80,25 75,40 75,50\" class=\"sq-edge\"/><text x=\"75\" y=\"20\" class=\"sq-lbl\" style=\"font-size:9px\">Push</text><path d=\"M125,50 C125,40 120,25 105,25\" class=\"sq-edge\"/><text x=\"130\" y=\"20\" class=\"sq-lbl\" style=\"font-size:9px\">Pop</text><!-- QUEUE --><text x=\"300\" y=\"15\" class=\"sq-lbl-title\">QUEUE (FIFO)</text><line x1=\"240\" y1=\"55\" x2=\"360\" y2=\"55\" stroke=\"var(--border-default)\" stroke-width=\"2\"/><line x1=\"240\" y1=\"95\" x2=\"360\" y2=\"95\" stroke=\"var(--border-default)\" stroke-width=\"2\"/><rect x=\"250\" y=\"65\" width=\"40\" height=\"20\" rx=\"3\" class=\"sq-box\"/><text x=\"270\" y=\"75\" class=\"sq-lbl-val\">Val 1</text><rect x=\"300\" y=\"65\" width=\"40\" height=\"20\" rx=\"3\" class=\"sq-box\"/><text x=\"320\" y=\"75\" class=\"sq-lbl-val\">Val 2</text><line x1=\"220\" y1=\"75\" x2=\"235\" y2=\"75\" class=\"sq-edge\"/><text x=\"210\" y=\"65\" class=\"sq-lbl\" style=\"font-size:9px\">Enqueue</text><line x1=\"345\" y1=\"75\" x2=\"375\" y2=\"75\" class=\"sq-edge\"/><text x=\"380\" y=\"65\" class=\"sq-lbl\" style=\"font-size:9px\">Dequeue</text></svg></div>",
      "codeBlock": "from collections import deque\n\n# Stack (LIFO) \u2014 use list\nstack = []\nstack.append(1)   # push O(1)\nstack.append(2)\nstack.append(3)\nprint(stack.pop())  # 3, O(1)\nprint(stack[-1])    # peek without pop\n\n# Queue (FIFO) \u2014 use deque, NOT list\nqueue = deque()\nqueue.append(1)        # enqueue O(1)\nqueue.append(2)\nprint(queue.popleft()) # dequeue O(1)\n\n# Deque as both stack and queue\ndq = deque()\ndq.append(1)       # right push O(1)\ndq.appendleft(0)   # left push O(1)\ndq.pop()           # right pop O(1)\ndq.popleft()       # left pop O(1)\n\n# Priority Queue (min-heap)\nimport heapq\npq = []\nheapq.heappush(pq, (1, 'high priority'))\nheapq.heappush(pq, (3, 'low priority'))\nprint(heapq.heappop(pq))  # (1, 'high priority')"
    },
    {
      "title": "Min Stack \u2014 O(1) getMin",
      "description": "Design a stack that supports push, pop, and getMin in O(1). The trick: maintain a parallel <em>auxiliary stack</em> that tracks the current minimum. When you pop, also pop from the auxiliary stack. This avoids O(N) scanning for the new minimum.",
      "codeBlock": "class MinStack:\n    def __init__(self):\n        self.stack = []      # main stack\n        self.min_stack = []  # auxiliary: tracks current min\n\n    def push(self, val: int) -> None:\n        self.stack.append(val)\n        # Push current min to min_stack (or val if min_stack is empty)\n        curr_min = min(val, self.min_stack[-1] if self.min_stack else val)\n        self.min_stack.append(curr_min)\n\n    def pop(self) -> None:\n        self.stack.pop()\n        self.min_stack.pop()   # always pop in sync\n\n    def top(self) -> int:\n        return self.stack[-1]\n\n    def get_min(self) -> int:\n        return self.min_stack[-1]  # O(1) \u2014 no scan\n\nms = MinStack()\nfor v in [5, 3, 7, 2, 8]:\n    ms.push(v)\n    print(f'pushed {v}, min={ms.get_min()}')\n# 5\u21925, 3\u21923, 7\u21923, 2\u21922, 8\u21922"
    },
    {
      "title": "Monotonic Stack \u2014 Next Greater / Smaller Element",
      "description": "Monotonic stacks maintain elements in sorted order. When an element violates the order, we pop \u2014 and each pop <em>answers a query</em>. Overall O(N) since each element is pushed and popped at most once.<br><br><strong>Pattern:</strong> 'Next greater/smaller element', 'Largest rectangle in histogram', 'Daily temperatures', 'Trapping rain water', 'Stock span'.",
      "codeBlock": "# Next Greater Element \u2014 O(N)\ndef next_greater(nums):\n    result = [-1] * len(nums)\n    stack = []  # monotonic decreasing (indices)\n    for i, num in enumerate(nums):\n        # Pop all elements smaller than current \u2014 current is their next greater\n        while stack and nums[stack[-1]] < num:\n            result[stack.pop()] = num\n        stack.append(i)\n    # Remaining in stack have no next greater \u2192 result stays -1\n    return result\n\n# Daily Temperatures (variant)\ndef daily_temperatures(temps):\n    result = [0] * len(temps)\n    stack = []  # indices\n    for i, t in enumerate(temps):\n        while stack and temps[stack[-1]] < t:\n            j = stack.pop()\n            result[j] = i - j   # days to wait\n        stack.append(i)\n    return result\n\n# Largest Rectangle in Histogram \u2014 O(N)\ndef largest_rectangle(heights):\n    heights.append(0)  # sentinel to flush remaining stack\n    stack = [-1]       # sentinel index\n    max_area = 0\n    for i, h in enumerate(heights):\n        while stack[-1] != -1 and heights[stack[-1]] >= h:\n            height = heights[stack.pop()]\n            width  = i - stack[-1] - 1\n            max_area = max(max_area, height * width)\n        stack.append(i)\n    return max_area"
    },
    {
      "title": "Queue Using Two Stacks",
      "description": "Implement a queue using only two stacks. Amortized O(1) per operation \u2014 each element is moved from stack1 to stack2 at most once. A classic interview question testing amortized analysis reasoning.",
      "codeBlock": "class MyQueue:\n    def __init__(self):\n        self.in_stack  = []   # for push\n        self.out_stack = []   # for pop/peek\n\n    def push(self, x: int) -> None:\n        self.in_stack.append(x)   # always push to in_stack O(1)\n\n    def _transfer(self):\n        if not self.out_stack:    # lazy transfer \u2014 only when out is empty\n            while self.in_stack:\n                self.out_stack.append(self.in_stack.pop())\n\n    def pop(self) -> int:\n        self._transfer()\n        return self.out_stack.pop()  # O(1) amortized\n\n    def peek(self) -> int:\n        self._transfer()\n        return self.out_stack[-1]    # O(1) amortized\n\n    def empty(self) -> bool:\n        return not self.in_stack and not self.out_stack\n\n# Amortized analysis:\n# Each element: pushed once to in_stack, moved once to out_stack, popped once\n# \u2192 3 operations total \u2192 O(1) amortized per push/pop"
    },
    {
      "title": "Sliding Window Maximum \u2014 Monotonic Deque",
      "description": "Find the maximum of every sliding window of size K. Brute force is O(NK). Monotonic deque achieves O(N) by keeping a decreasing deque of indices \u2014 the front is always the window maximum.",
      "codeBlock": "from collections import deque\n\ndef max_sliding_window(nums, k):\n    \"\"\"\n    Deque stores indices in decreasing order of nums[i].\n    Front = index of current window maximum.\n    \"\"\"\n    dq = deque()  # stores indices, front = max index\n    result = []\n\n    for i, num in enumerate(nums):\n        # Remove indices outside window\n        while dq and dq[0] < i - k + 1:\n            dq.popleft()\n\n        # Remove smaller elements from back (they'll never be the max)\n        while dq and nums[dq[-1]] < num:\n            dq.pop()\n\n        dq.append(i)\n\n        # Window is full \u2014 record maximum (front of deque)\n        if i >= k - 1:\n            result.append(nums[dq[0]])\n\n    return result\n\n# Example\nprint(max_sliding_window([1,3,-1,-3,5,3,6,7], k=3))\n# [3, 3, 5, 5, 6, 7]"
    },
    {
      "title": "Staff+ — Concurrent Queues at Scale",
      "description": "<strong>MPMC queues</strong> (Multiple Producer, Multiple Consumer): Python's <code>queue.Queue</code> is thread-safe (uses <code>threading.Lock</code> + <code>Condition</code>). For high-throughput, use <code>collections.deque</code> with <code>asyncio.Queue</code> or a lock-free ring buffer. At Google scale, queues back every RPC, task scheduler, and build pipeline.<br><br><strong>Ring buffer (circular buffer):</strong> Fixed-size pre-allocated array with head/tail pointers. O(1) enqueue/dequeue, zero GC pressure. Used in kernel io_uring, Kafka partitions, and Log-structured merge-trees.<br><br><strong>Lock-free queues:</strong> Hazard pointers or RCU for safe concurrent access without mutexes. Python's GIL makes true lock-freedom unnecessary within a process, but cross-process (shared memory) or cross-network queues need real lock-free or transaction-log semantics.<br><br><strong>Backpressure & bounded queues:</strong> Unbounded queues hide backpressure and crash under spike load. Always bound your queue. Use <code>queue.Queue(maxsize=N)</code>; when full, the producer blocks or raises. Tail-drop, head-drop, or TTL-based eviction for real-time systems.",
      "codeBlock": "from collections import deque\n\n# Ring buffer skeleton\nclass RingBuffer:\n    def __init__(self, capacity):\n        self.buf = [None] * capacity\n        self.cap = capacity\n        self.head = 0  # read position\n        self.tail = 0  # write position\n        self.size = 0\n\n    def enqueue(self, item):\n        if self.size == self.cap:\n            raise OverflowError('Ring buffer full')\n        self.buf[self.tail] = item\n        self.tail = (self.tail + 1) % self.cap\n        self.size += 1\n\n    def dequeue(self):\n        if self.size == 0:\n            raise IndexError('Empty')\n        item = self.buf[self.head]\n        self.head = (self.head + 1) % self.cap\n        self.size -= 1\n        return item\n\n    def __len__(self):\n        return self.size\n"
    },
    {
      "title": "References & Further Reading",
      "description": "<ul><li><a href=\"https://docs.python.org/3/tutorial/datastructures.html#using-lists-as-stacks\"><b>Using Lists as Stacks</b></a> — Python list as stack (LIFO) documentation</li><li><a href=\"https://docs.python.org/3/library/collections.html#collections.deque\"><b>deque — Efficient Stacks and Queues</b></a> — O(1) queue operations with collections.deque</li><li><a href=\"https://docs.python.org/3/library/queue.html\"><b>queue — Synchronized queue class</b></a> — Thread-safe queues for concurrent programming</li><li><a href=\"https://realpython.com/queue-in-python/\"><b>Python Stacks, Queues, and Priority Queues</b></a> — Real Python comprehensive guide</li><li><a href=\"https://leetcode.com/tag/stack/\"><b>LeetCode — Stack Problems</b></a> — Curated stack interview problems (monotonic stack, etc.)</li></ul>",
      "codeBlock": ""
    }
  ],
  "details": "<p class=\"mb-3\"><strong>Monotonic Stack/Queue Recognition Pattern:</strong> If the brute force involves an inner loop scanning left or right to find the next/previous smaller/larger element, a monotonic stack turns it O(N). If it involves scanning a window for min/max, a monotonic deque turns it O(N). These are table-stakes optimizations at FAANG L5+.</p><p class=\"mb-3\"><strong>Stack for Parsing:</strong> Stacks are natural for balanced brackets, expression evaluation, and recursive structure parsing. The call stack itself is a stack \u2014 iterative DFS uses an explicit one. Any time you need 'match this opening thing with its closing thing', think stack.</p><p class=\"mb-3\"><strong>Deque as Two-Ended Sliding Window:</strong> The sliding window maximum deque pattern generalizes. BFS uses deque naturally. The double-ended property enables sliding window problems where you need O(1) operations on both ends simultaneously.</p>",
  "codeBlock": "",
  "tags": [
    "FAANG",
    "Python",
    "data-structures",
    "stack-queue",
    "Staff+",
    "concurrent-queues",
    "ring-buffer"
  ]
}