{
  "id": "genai-llm-arch",
  "title": "LLM Architecture: End-to-End Component Guide",
  "category": "Gen AI",
  "subcategory": "Architecture",
  "description": "A detailed walkthrough of the transformer-based LLM architecture — from tokenized input to output probabilities — explaining the function of every component in the inference pipeline.",
  "language": "python",
  "sections": [
    {
      "title": "End-to-End Architecture Diagram",
      "description": "<div class=\"my-4 flex justify-center overflow-x-auto\"><svg width=\"580\" height=\"720\" viewBox=\"0 0 580 720\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" class=\"max-w-full h-auto\"><defs><marker id=\"arrow\" viewBox=\"0 0 10 10\" refX=\"9\" refY=\"5\" markerWidth=\"6\" markerHeight=\"6\" orient=\"auto\"><path d=\"M 0 0 L 10 5 L 0 10 z\" class=\"fill-slate-400 dark:fill-slate-500\"/></marker></defs><text x=\"290\" y=\"20\" text-anchor=\"middle\" class=\"fill-slate-800 dark:fill-slate-200 font-sans text-xs font-bold uppercase tracking-wider\">LLM Architecture (Decoder-Only Transformer)</text><!-- Input --><rect x=\"190\" y=\"35\" width=\"200\" height=\"32\" rx=\"6\" class=\"fill-slate-50 dark:fill-slate-800/40 stroke-slate-300 dark:stroke-slate-700\" stroke-width=\"1.5\"/><text x=\"290\" y=\"55\" text-anchor=\"middle\" class=\"fill-slate-700 dark:fill-slate-300 font-mono text-[10px]\">Input Tokens [1 &times; seq_len]</text><line x1=\"290\" y1=\"67\" x2=\"290\" y2=\"82\" class=\"stroke-slate-400 dark:stroke-slate-500\" stroke-width=\"1.5\" marker-end=\"url(#arrow)\"/><!-- Token Embedding --><rect x=\"190\" y=\"85\" width=\"200\" height=\"32\" rx=\"6\" class=\"fill-orange-50 dark:fill-orange-950/20 stroke-brand-500\" stroke-width=\"1.5\"/><text x=\"290\" y=\"105\" text-anchor=\"middle\" class=\"fill-brand-600 dark:fill-brand-400 font-mono text-[10px] font-bold\">Token + Positional Embeddings</text><text x=\"395\" y=\"100\" class=\"fill-slate-400 dark:fill-slate-500 text-[9px] font-mono\">d_model</text><line x1=\"290\" y1=\"117\" x2=\"290\" y2=\"132\" class=\"stroke-slate-400 dark:stroke-slate-500\" stroke-width=\"1.5\" marker-end=\"url(#arrow)\"/><!-- Transformer Block container --><rect x=\"140\" y=\"135\" width=\"300\" height=\"380\" rx=\"10\" class=\"fill-slate-50/50 dark:fill-slate-800/20 stroke-slate-200 dark:stroke-slate-800\" stroke-width=\"1\" stroke-dasharray=\"4 3\"/><text x=\"290\" y=\"152\" text-anchor=\"middle\" class=\"fill-slate-500 dark:fill-slate-400 text-[9px] font-mono font-bold uppercase tracking-wider\">Transformer Block &times; N (e.g., 32 layers)</text><!-- Sub-layer 1: Attention --><rect x=\"170\" y=\"165\" width=\"240\" height=\"35\" rx=\"5\" class=\"fill-white dark:fill-slate-900/60 stroke-brand-500\" stroke-width=\"1.5\"/><text x=\"290\" y=\"178\" text-anchor=\"middle\" class=\"fill-slate-800 dark:fill-slate-200 font-sans text-[10px] font-semibold\">Multi-Head Self-Attention</text><text x=\"290\" y=\"192\" text-anchor=\"middle\" class=\"fill-slate-400 dark:fill-slate-500 text-[8px] font-mono\">Q = xW_Q &middot; K = xW_K &middot; V = xW_V &middot; Attn = Softmax(QK&#8315;&#8260;&#8730;d_k) &middot; Output = Attn &times; V</text><!-- Residual Add + Norm --><rect x=\"170\" y=\"208\" width=\"180\" height=\"22\" rx=\"4\" class=\"fill-orange-50 dark:fill-orange-950/20 stroke-orange-200 dark:stroke-orange-800\" stroke-width=\"1\"/><text x=\"260\" y=\"222\" text-anchor=\"middle\" class=\"fill-brand-600 dark:fill-brand-400 text-[8px] font-mono font-bold\">+ Residual &rarr; LayerNorm</text><!-- Side residual arrow --><path d=\"M 165 182 C 130 182 130 230 165 219\" class=\"stroke-slate-300 dark:stroke-slate-600\" stroke-width=\"1\" stroke-dasharray=\"3 2\" fill=\"none\" marker-end=\"url(#arrow)\"/><line x1=\"290\" y1=\"230\" x2=\"290\" y2=\"248\" class=\"stroke-slate-400 dark:stroke-slate-500\" stroke-width=\"1.5\" marker-end=\"url(#arrow)\"/><!-- Sub-layer 2: FFN --><rect x=\"170\" y=\"250\" width=\"240\" height=\"35\" rx=\"5\" class=\"fill-white dark:fill-slate-900/60 stroke-brand-500\" stroke-width=\"1.5\"/><text x=\"290\" y=\"263\" text-anchor=\"middle\" class=\"fill-slate-800 dark:fill-slate-200 font-sans text-[10px] font-semibold\">Feed-Forward Network (MLP)</text><text x=\"290\" y=\"277\" text-anchor=\"middle\" class=\"fill-slate-400 dark:fill-slate-500 text-[8px] font-mono\">SwiGLU(x) = (xW_1 &odot; SiLU(xW_g)) &middot; W_2 &nbsp;|&nbsp; dim: d_model &rarr; 4d &rarr; d_model</text><!-- Residual Add + Norm --><rect x=\"170\" y=\"293\" width=\"180\" height=\"22\" rx=\"4\" class=\"fill-orange-50 dark:fill-orange-950/20 stroke-orange-200 dark:stroke-orange-800\" stroke-width=\"1\"/><text x=\"260\" y=\"307\" text-anchor=\"middle\" class=\"fill-brand-600 dark:fill-brand-400 text-[8px] font-mono font-bold\">+ Residual &rarr; LayerNorm</text><!-- Side residual arrow --><path d=\"M 165 267 C 130 267 130 315 165 304\" class=\"stroke-slate-300 dark:stroke-slate-600\" stroke-width=\"1\" stroke-dasharray=\"3 2\" fill=\"none\" marker-end=\"url(#arrow)\"/><line x1=\"290\" y1=\"315\" x2=\"290\" y2=\"340\" class=\"stroke-slate-400 dark:stroke-slate-500\" stroke-width=\"1.5\"/><text x=\"290\" y=\"352\" text-anchor=\"middle\" class=\"fill-slate-400 dark:fill-slate-500 text-[10px] font-mono\">&vellip; Repeated N times</text><line x1=\"290\" y1=\"365\" x2=\"290\" y2=\"390\" class=\"stroke-slate-400 dark:stroke-slate-500\" stroke-width=\"1.5\"/><!-- KV Cache callout --><rect x=\"410\" y=\"170\" width=\"150\" height=\"75\" rx=\"6\" class=\"fill-blue-50 dark:fill-blue-950/20 stroke-blue-300 dark:stroke-blue-700\" stroke-width=\"1\" stroke-dasharray=\"3 2\"/><text x=\"485\" y=\"187\" text-anchor=\"middle\" class=\"fill-blue-600 dark:fill-blue-400 text-[8px] font-mono font-bold\">KV Cache</text><text x=\"485\" y=\"200\" text-anchor=\"middle\" class=\"fill-blue-500 dark:fill-blue-400 text-[7px] font-mono\">Caches K,V from</text><text x=\"485\" y=\"212\" text-anchor=\"middle\" class=\"fill-blue-500 dark:fill-blue-400 text-[7px] font-mono\">previous tokens</text><text x=\"485\" y=\"224\" text-anchor=\"middle\" class=\"fill-blue-500 dark:fill-blue-400 text-[7px] font-mono\">O(seq) per step</text><line x1=\"410\" y1=\"200\" x2=\"360\" y2=\"200\" class=\"stroke-blue-300 dark:stroke-blue-700\" stroke-width=\"1\" stroke-dasharray=\"2 2\" fill=\"none\" marker-end=\"url(#arrow)\"/><!-- Output from last block --><line x1=\"290\" y1=\"515\" x2=\"290\" y2=\"540\" class=\"stroke-slate-400 dark:stroke-slate-500\" stroke-width=\"1.5\" marker-end=\"url(#arrow)\"/><!-- LM Head --><rect x=\"165\" y=\"545\" width=\"250\" height=\"32\" rx=\"6\" class=\"fill-orange-50 dark:fill-orange-950/20 stroke-brand-500\" stroke-width=\"1.5\"/><text x=\"290\" y=\"565\" text-anchor=\"middle\" class=\"fill-brand-600 dark:fill-brand-400 font-mono text-[10px] font-bold\">LM Head: LayerNorm &rarr; Linear Projection</text><text x=\"470\" y=\"565\" class=\"fill-slate-400 dark:fill-slate-500 text-[9px] font-mono\">d_model &rarr; vocab</text><line x1=\"290\" y1=\"577\" x2=\"290\" y2=\"592\" class=\"stroke-slate-400 dark:stroke-slate-500\" stroke-width=\"1.5\" marker-end=\"url(#arrow)\"/><!-- Softmax --><rect x=\"165\" y=\"595\" width=\"250\" height=\"32\" rx=\"6\" class=\"fill-white dark:fill-slate-900/60 stroke-slate-300 dark:stroke-slate-700\" stroke-width=\"1.5\"/><text x=\"290\" y=\"615\" text-anchor=\"middle\" class=\"fill-slate-700 dark:fill-slate-300 font-mono text-[10px]\">Softmax &rarr; Probabilities [1 &times; vocab_size]</text><line x1=\"290\" y1=\"627\" x2=\"290\" y2=\"642\" class=\"stroke-slate-400 dark:stroke-slate-500\" stroke-width=\"1.5\" marker-end=\"url(#arrow)\"/><!-- Output token --><rect x=\"190\" y=\"645\" width=\"200\" height=\"32\" rx=\"16\" class=\"fill-brand-500/10 stroke-brand-500\" stroke-width=\"2\"/><text x=\"290\" y=\"665\" text-anchor=\"middle\" class=\"fill-brand-600 dark:fill-brand-400 font-mono text-[11px] font-bold\">Next Token (Argmax / Sample)</text><!-- Arrow label annotations --><text x=\"305\" y=\"435\" class=\"fill-slate-400 dark:fill-slate-500 text-[8px] font-mono\">hidden states</text><text x=\"305\" y=\"500\" class=\"fill-slate-400 dark:fill-slate-500 text-[8px] font-mono\">hidden states</text></svg></div>",
      "codeBlock": ""
    },
    {
      "title": "1. Tokenizer & Input Embeddings",
      "description": "The <strong>tokenizer</strong> converts raw text into a sequence of integer token IDs using a subword vocabulary (e.g., BPE or SentencePiece). Each token ID is mapped to a dense vector via an <strong>embedding lookup table</strong> of shape <code>[vocab_size, d_model]</code>. A <strong>positional encoding</strong> (sinusoidal or learned) is added to inject sequence position information, since self-attention has no inherent notion of order.",
      "codeBlock": "# Simplified embedding lookup + positional encoding\ndef embed_input(token_ids, embed_weight, max_len, d_model):\n    B, T = token_ids.shape\n    # Token embeddings: [B, T, d_model]\n    tok_emb = embed_weight[token_ids]\n    # Positional encoding: [1, max_len, d_model]\n    pos = torch.arange(T).unsqueeze(0)\n    pos_emb = sinusoidal_positional_encoding(pos, d_model)\n    return tok_emb + pos_emb"
    },
    {
      "title": "2. Multi-Head Self-Attention",
      "description": "Each token's hidden state is linearly projected into three matrices: <strong>Query (Q)</strong>, <strong>Key (K)</strong>, and <strong>Value (V)</strong>. The attention score between every pair of tokens is computed as <code>Softmax(QK&#8315;&#8260;&#8730;d_k)</code>, producing a weighted sum of Values. This is split across <strong>multiple heads</strong> (typically 8&ndash;96) that learn different relationship types (syntax, semantics, positions). The outputs are concatenated and linearly projected back to d_model.",
      "codeBlock": "def multi_head_attention(x, num_heads, d_model):\n    B, T, C = x.shape\n    head_dim = C // num_heads\n    \n    # Project to Q, K, V: [B, T, C]\n    Q = linear(x, W_Q)\n    K = linear(x, W_K)\n    V = linear(x, W_V)\n    \n    # Reshape for multi-head: [B, num_heads, T, head_dim]\n    Q = Q.view(B, T, num_heads, head_dim).transpose(1, 2)\n    K = K.view(B, T, num_heads, head_dim).transpose(1, 2)\n    V = V.view(B, T, num_heads, head_dim).transpose(1, 2)\n    \n    # Scaled dot-product attention\n    scores = Q @ K.transpose(-2, -1) / math.sqrt(head_dim)\n    attn = F.softmax(scores, dim=-1)  # [B, h, T, T]\n    \n    # Weighted sum of Values\n    out = attn @ V  # [B, h, T, d_h]\n    \n    # Concatenate heads and project\n    out = out.transpose(1, 2).reshape(B, T, C)\n    return linear(out, W_O)"
    },
    {
      "title": "3. Feed-Forward Network (SwiGLU MLP)",
      "description": "Each token's hidden state passes independently through a two-layer MLP that expands the dimension by a factor of &times;4 (or &times;8/3 in SwiGLU). Modern LLMs use the <strong>SwiGLU</strong> activation: a gated variant where one path passes through SiLU (Swish) activation and is element-wise multiplied by another path. This accounts for roughly &frac23; of the model's parameters and compute.<br><br><strong>Dimensions:</strong> <code>d_model &rarr; intermediate_size (4d) &rarr; d_model</code>",
      "codeBlock": "def swiglu_ffn(x, W_gate, W_up, W_down):\n    # Gated linear unit with SiLU activation\n    gate = F.silu(linear(x, W_gate))  # gating path\n    up = linear(x, W_up)              # value path\n    hidden = gate * up                 # element-wise gate\n    return linear(hidden, W_down)      # project back to d_model"
    },
    {
      "title": "4. Residual Connections & Layer Normalization",
      "description": "Every sub-layer (Attention and FFN) is wrapped in a <strong>residual connection</strong> followed by <strong>Layer Normalization</strong>. The residual path (<code>output = sublayer(norm(x)) + x</code>) allows gradients to flow directly through the network, preventing vanishing gradients in deep models. Pre-LayerNorm (applying norm before the sub-layer) is the standard in modern LLMs as it stabilizes training.",
      "codeBlock": "def transformer_block(x, attn, ffn, norm1, norm2):\n    # Pre-LayerNorm architecture (modern standard)\n    # Sub-layer 1: Multi-Head Self-Attention\n    x = x + attn(norm1(x))\n    \n    # Sub-layer 2: Feed-Forward Network\n    x = x + ffn(norm2(x))\n    \n    return x"
    },
    {
      "title": "5. LM Head & Output Sampling",
      "description": "The final hidden state of the last transformer block passes through a <strong>LayerNorm</strong> and a <strong>linear projection</strong> (the LM Head) mapping from <code>d_model</code> to <code>vocab_size</code> logits. <strong>Softmax</strong> converts these logits into a probability distribution over the vocabulary. The next token is selected via <strong>argmax</strong> (greedy), <strong>top-k</strong>, <strong>top-p</strong> (nucleus), or <strong>temperature</strong>-scaled sampling strategies.",
      "codeBlock": "def lm_head(hidden_states, ln_final, head_weight):\n    # Final layer norm\n    h = ln_final(hidden_states[:, -1, :])  # only last token\n    \n    # Linear projection to vocabulary\n    logits = h @ head_weight.T  # [1, vocab_size]\n    \n    # Temperature-scaled sampling\n    probs = F.softmax(logits / temperature, dim=-1)\n    \n    # Sample from distribution (nucleus/top-k)\n    next_token = sample_from_probs(probs, top_k=50, top_p=0.9)\n    return next_token"
    },
    {
      "title": "6. KV Cache Integration",
      "description": "During auto-regressive decoding, the <strong>KV Cache</strong> stores the Key and Value matrices from all previous attention steps. For each new token, only the new token's Q, K, V are computed, and the cached K, V are reused from memory instead of being recomputed across all previous positions. This reduces the per-step attention complexity from <code>O(T&sup2;)</code> to <code>O(T)</code>, but at the cost of <code>O(N &times; L &times; d)</code> memory per sequence where N = num_heads, L = num_layers, d = head_dim.",
      "codeBlock": "def decode_with_kv_cache(token, past_kv, model):\n    # past_kv holds cached K,V for all previous tokens\n    \n    # Compute Q, K, V only for the new token\n    q = model.W_q(token)   # [1, 1, d_model]\n    k = model.W_k(token)   # [1, 1, d_model]\n    v = model.W_v(token)   # [1, 1, d_model]\n    \n    # Append to cache\n    k = torch.cat([past_kv['k'], k], dim=1)\n    v = torch.cat([past_kv['v'], v], dim=1)\n    past_kv = {'k': k, 'v': v}\n    \n    # Attention over ALL cached positions (O(T) per step)\n    scores = q @ k.transpose(-2, -1) / math.sqrt(d_k)\n    attn = F.softmax(scores, dim=-1)\n    out = attn @ v\n    return out, past_kv"
    }
  ],
  "details": "<p class=\"mb-3\"><strong>Key Design Principles:</strong> Modern LLMs follow the decoder-only transformer architecture popularized by GPT. The model is autoregressive — each token is generated one at a time, conditioned on all previous tokens. Scaling laws have shown consistent improvements with larger d_model, more layers, more data, and more compute. Key innovations include <strong>Pre-LayerNorm</strong> (GPT-2 onward), <strong>SwiGLU</strong> activations (PaLM, Llama), <strong>Rotary Positional Embedding (RoPE)</strong> (Llama, Mistral), and <strong>Grouped-Query Attention (GQA)</strong> (Llama 2, Mistral) which reduces KV cache memory by sharing K/V heads across Q heads.</p>"
}
